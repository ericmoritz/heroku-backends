from setuptools import setup

setup(
    name="heroku-backends",
    description="dirty hacks to connect to our common Heroku backends",
    version="0.1",
    packages=["heroku_backends"],
)

      
